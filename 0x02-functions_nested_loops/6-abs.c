#include "main.h"
/**
  * _abs - computes the absolute value of an integer
  * @n: computing the absolute value of it
  * Return: always n.
  */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
